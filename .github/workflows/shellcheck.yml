name: ShellCheck
on:
  push: { paths: ["*.sh", "**/*.sh"] }
  pull_request: { paths: ["*.sh", "**/*.sh"] }
jobs:
  shellcheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ludeeus/action-shellcheck@2.0.0

